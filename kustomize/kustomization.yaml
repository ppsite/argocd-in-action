apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - envs:
      - envs
    name: envs
secretGenerator:
  - envs:
      - envs
    name: secrets
generatorOptions:
  disableNameSuffixHash: true